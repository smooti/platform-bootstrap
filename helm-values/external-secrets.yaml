# nstall and upgrade CRDs 
installCRDs: true

crds:
  # Create CRDs for Cluster External Secret.
  createClusterExternalSecret: true
  # -- Create CRDs for Cluster Secret Store.
  createClusterSecretStore: true
  # -- Create CRDs for Cluster Generator.
  createClusterGenerator: true
  # -- Create CRDs for Cluster Push Secret.
  createClusterPushSecret: true
  # -- Create CRDs for Push Secret.
  createPushSecret: true